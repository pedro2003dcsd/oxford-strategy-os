import { CarteraClientes } from "@/components/prototipo/CarteraClientes";

export const metadata = {
  title: "Cartera de Clientes · Oxford Strategy OS",
};

/** Prototipo: los datos son de maqueta, no salen de la base. */
export default function ClientesPage() {
  return <CarteraClientes />;
}
